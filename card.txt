ListView.builder(
        itemCount: widget.listArticles.length,

        itemBuilder: (BuildContext context, int index) {

          return ListTile(
          leading: Image.network(
            widget.listArticles[index].image,
            width: 50,
            height: 50,
          ),
          title: Text(widget.listArticles[index].title),
          subtitle: Text('\$${widget.listArticles[index].price}'),
          trailing: IconButton(
            icon: Icon(Icons.add_shopping_cart),
            onPressed: () {
              // Ajouter le produit au panier ici
            },
          ),
        );
        },
      );

      flutter pub add shared_preferences: ^2.0.13
